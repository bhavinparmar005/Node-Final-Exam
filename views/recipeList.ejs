<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>All Recipes — RecipeShare</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <%- include('navbar') %>

    <main class="container">
      <header class="page-header">
        <h1>All Recipes</h1>
        <p class="muted">Discover recipes shared by the community.</p>
      </header>

      <section class="recipe-list">
        <% if (!recipes || recipes.length === 0) { %>
        <p>
          No recipes yet — be the first to <a href="/recipes/add">add one</a>!
        </p>
        <% } else { %> <% recipes.forEach(function(recipe) { %> <%-
        include('recipeItem', { recipe: recipe }) %> <% }) %> <% } %>
      </section>
    </main>
  </body>
</html>
